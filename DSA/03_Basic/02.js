// Factorial number
/*
let num = 500;

loop runs on only half times 
beacuse ( ** asume num is 25 so no natural number which divide in above 12 else 25 it self )

for(let i = 1; i <= Math.floor(num/2); i++) { 
    if(num%i === 0) {
        console.log({ i })
    }
}
console.log({ num })
*/

/* ============================================================= */

// prime number
/*
const isPrim = (n) => {
    if( n <= 1 ) return false;
    if( n == 2 ) return true;
    if( n == 2 ) return false;
    for ( let i = 3; i < Math.floor(Math.sqrt(n)); i+=2) {
        if( n%i === 0) return false;
    }
    return true
}

let n = 33;

console.log(isPrim(n))
*/

/* ========================================================= */

