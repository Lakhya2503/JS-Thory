// Revers Array
/*
let array = [ 1, 2, 3, 4, 5]
let newArray = new Array(array.length)

let j = 0

for (let i = array.length - 1; i >= 0; i--) {
    newArray[j] = array[i]
    j++
}

console.log({ newArray })
*/
/* ======================================================================== */